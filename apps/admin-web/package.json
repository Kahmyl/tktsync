{
  "name": "@tktsync/admin-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "@tktsync/ui": "workspace:*",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
